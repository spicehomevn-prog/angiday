Rounded text field; red focus border. Add the `.agd-input:focus` rule via `styles.css` so focus turns the border red.

```jsx
<Input label="Tên gia đình" placeholder="VD: nhà Bống..." />
<Input inputmode="numeric" placeholder="Ngân sách / ngày" />
```

- Off-white `--surface-sunken` fill, 13px radius, 2px hairline border.
