Big quantity stepper; large Baloo number flanked by round soft buttons.

```jsx
<Stepper value={people} onChange={setPeople} min={1} max={20} unit="người" />
```

- Caption (`unit`) sits centred under the number in muted taupe.
- Values clamp to `[min, max]`.
