Small soft-tint label pill; lead with an emoji as on brand.

```jsx
<Badge tone="gold">🍗 Đủ đạm</Badge>
<Badge tone="green">🥬 Có rau xanh</Badge>
<Badge tone="plum">🦴 Giàu canxi</Badge>
```

- `tone`: `gold` · `green` · `plum` · `red`. Each is a pale fill with readable accent text.
- Group several in a wrapping flex row with ~7px gap to summarise a menu.
